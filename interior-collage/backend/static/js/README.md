# 📁 Структура модулей приложения Interior Collage Builder

Это документация для новичков. Здесь объясняется, за что отвечает каждый файл.

---

## 🗂️ Общая структура

```
static/
├── js/
│   ├── api.js           → Запросы к серверу
│   ├── canvas.js        → Холст Konva (сцена)
│   ├── perspective.js   → Режим "Транспортировка" с якорями
│   ├── eraser.js        → Инструмент "Ластик"
│   ├── layers.js        → Панель слоёв справа
│   ├── catalog.js       → Каталог товаров слева
│   └── utils.js         → Вспомогательные функции
├── app.js              → ГЛАВНЫЙ ФАЙЛ (объединяет всё)
├── index.html          → HTML-страница
└── styles.css          → Стили
```

---

## 📄 Описание каждого файла

### 1️⃣ **utils.js** — Вспомогательные функции
**Зачем:** Здесь живут функции, которые используются во многих местах.

**Что внутри:**
- `debounce()` — задержка для поиска (чтобы не дергать сервер при каждом символе)
- `getImageGroups()` — получить все изображения на холсте

**Когда редактировать:**
- Если нужна какая-то общая функция для всего приложения.

---

### 2️⃣ **api.js** — Работа с сервером
**Зачем:** Все запросы к серверу в одном месте.

**Что внутри:**
- `API.products()` — получить список товаров
- `API.categories()` — получить категории товаров

**Когда редактировать:**
- Если нужно добавить новый запрос к серверу (например, сохранение коллажа на сервер).

---

### 3️⃣ **canvas.js** — Холст Konva
**Зачем:** Это основа приложения — холст, на котором всё рисуется.

**Что внутри:**
- Создание `stage` (сцена Konva)
- Создание `layer` (слой для изображений)
- Создание `transformer` (рамка вокруг выделенного объекта)
- Обработка кликов на холсте

**Когда редактировать:**
- Если нужно изменить поведение холста (например, добавить zoom, grid, и т.д.)

---

### 4️⃣ **perspective.js** — Режим "Транспортировка"
**Зачем:** Режим для искажения изображений с помощью 4 якорей (перспектива).

**Что внутри:**
- Переключение режима транспортировки
- Показ/скрытие якорей
- Перспективная трансформация изображения
- Создание изображений на холсте (`createImage()`)
- Добавление изображений на холст (`addImageToCanvas()`)

**Когда редактировать:**
- Если нужно изменить поведение режима транспортировки
- Если нужно добавить новые инструменты для изображений

---

### 5️⃣ **eraser.js** — Инструмент "Ластик"
**Зачем:** Стирание частей изображений.

**Что внутри:**
- Включение/выключение режима ластика
- Стирание пикселей изображения при клике и движении мыши
- Регулятор размера ластика

**Когда редактировать:**
- Если нужно улучшить ластик (например, добавить разные формы кисти)

---

### 6️⃣ **layers.js** — Панель слоёв
**Зачем:** Управление слоями (как в Photoshop).

**Что внутри:**
- Отображение списка слоёв
- Выбор слоя по клику
- Копирование слоя
- Перемещение слоя вверх/вниз

**Когда редактировать:**
- Если нужно добавить новые функции для слоёв (например, переименование, скрытие)

---

### 7️⃣ **catalog.js** — Каталог товаров
**Зачем:** Боковая панель слева с товарами.

**Что внутри:**
- Отображение товаров
- Поиск по товарам
- Фильтр по категориям
- Клик на товар → добавление на холст

**Когда редактировать:**
- Если нужно изменить внешний вид каталога или добавить фильтры

---

### 8️⃣ **app.js** — ГЛАВНЫЙ ФАЙЛ
**Зачем:** Точка входа. Объединяет все модули и запускает приложение.

**Что внутри:**
- Обработчики кнопок ("Очистить", "Экспорт", "Удалить")
- Инициализация приложения при загрузке страницы

**Когда редактировать:**
- Если нужно добавить новую кнопку или глобальный обработчик

---

## 🔄 Как модули работают вместе?

1. **index.html** подключает все модули по порядку (порядок важен!)
2. Каждый модуль добавляет свои функции в `window` (глобально)
3. **app.js** запускает инициализацию всех модулей
4. Модули общаются через глобальные переменные (например, `window.canvasLayer`)

---

## ➕ Как добавить новую функцию?

### Пример: Добавить кнопку "Отменить действие" (Undo)

**Шаг 1:** Создай новый модуль `js/history.js`
```javascript
// Модуль для истории действий
window.history = [];

function saveState() {
  // Сохранить текущее состояние холста
}

function undo() {
  // Вернуться к предыдущему состоянию
}
```

**Шаг 2:** Подключи модуль в `index.html`
```html
<script src="/static/js/history.js"></script>
```

**Шаг 3:** Добавь кнопку в `app.js`
```javascript
btnUndo.addEventListener('click', undo);
```

---

## 🐛 Что делать, если что-то сломалось?

1. Открой **консоль браузера** (F12)
2. Посмотри на ошибки
3. Обрати внимание на **имя файла и строку** с ошибкой
4. Проверь, что все модули подключены в правильном порядке в `index.html`
5. Проверь, что функции, которые ты вызываешь, существуют

---

## 💡 Советы для новичков

✅ **Всегда добавляй комментарии** к своему коду  
✅ **Называй функции понятно** (не `f1()`, а `addImageToCanvas()`)  
✅ **Один модуль = одна фича** (не смешивай всё в одном файле)  
✅ **Перед изменениями сделай копию файла** (на всякий случай)  

---

## 📚 Дополнительно

- [Документация Konva.js](https://konvajs.org/)
- [JavaScript для новичков](https://learn.javascript.ru/)

---

Удачи в разработке! 🚀

